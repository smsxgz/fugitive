[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fugitive-openspiel"
version = "0.2.0"
description = "C++ OpenSpiel implementation and local Web UI for the Fugitive card game"
readme = "README.md"
requires-python = ">=3.11"
authors = [
  { name = "Fugitive OpenSpiel project" },
]
dependencies = []

[project.optional-dependencies]
test = ["pytest>=8"]

[project.scripts]
fugitive = "fugitive.web.server:main"
fugitive-web = "fugitive.web.server:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"fugitive.web" = ["static/*"]

[tool.pytest.ini_options]
addopts = "-q"
pythonpath = ["src", "."]
testpaths = ["tests"]
