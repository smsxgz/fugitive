"""Policy-Space Response Oracles for full-game strategy experiments.

The package keeps policy populations, empirical payoffs, the meta solver, the
PSRO loop, full-game evaluation, and checkpointed experiment orchestration in
separate modules.  Import from the module that owns each concept so those
dependencies remain visible in teaching code.
"""
